// Schedule the cron job to run every 5 minutes
// const cronExpression1 = "*/1 * * * *";
// cron.schedule(cronExpression1, createGame);

// const cronExpression3 = "50/50 * * * * *";
// cron.schedule(cronExpression3, generateWinner);

// Inside the generateWinner function, check if 4 minutes and 55 seconds have passed
